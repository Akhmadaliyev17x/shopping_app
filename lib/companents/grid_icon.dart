import 'package:flutter/material.dart';

class GridIcon extends StatelessWidget {
  const GridIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return Icon(
      Icons.grid_view_rounded,
      color: Colors.grey,
      size: 25,
    );
  }
}
